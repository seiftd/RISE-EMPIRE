{
  "name": "tk",
  "version": "1.0.0",
  "description": "A comprehensive web application for generating motivational YouTube content for the Rise Empire channel. This tool creates everything you need for viral motivational shorts including scripts, image prompts, music suggestions, and SEO optimization.",
  "main": "rise-empire-agent.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/ 2>/dev/null || true",
    "start": "node server.js",
    "dev": "node server.js",
    "netlify-build": "mkdir -p dist && cp index.html dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["motivation", "youtube", "video", "content", "generation"],
  "author": "Rise Empire",
  "license": "ISC",
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.14.0",
    "@google/generative-ai": "^0.24.1",
    "canvas": "^3.2.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^2.0.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
